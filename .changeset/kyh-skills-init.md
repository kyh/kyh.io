---
"@kyh/skills": minor
---

Add `@kyh/skills`: distribute personal Claude Code + Codex skills, agents, MCP servers, and global instructions as an npm package. `postinstall` symlinks skills/agents/CLAUDE.md into `~/.claude`, generates Codex agent TOMLs into `~/.codex/agents`, and merges MCP servers into `~/.claude.json`.
