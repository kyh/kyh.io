{
  "$comment": "MCP servers listed here merge into ~/.claude.json (mcpServers) on install and run in EVERY project once linked globally. Empty by design — only add a server you genuinely want everywhere; otherwise configure it per-project.",
  "mcpServers": {}
}
