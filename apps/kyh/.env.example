# Copy to .env and fill in. Optional — the app runs without it, but project
# images and favicons (served from the Supabase public storage bucket) 404.
NEXT_PUBLIC_SUPABASE_URL="https://<project>.supabase.co"
