# Copy to .env and fill in. Optional — the app runs without it. Project images
# and favicons live in the public `kyh-assets` Vercel Blob store; with no URL set,
# src/lib/public-assets.ts falls back to a local placeholder (see AGENTS.md).
NEXT_PUBLIC_ASSETS_URL="https://x85gqgi8frl3abwt.public.blob.vercel-storage.com"
