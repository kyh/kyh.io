import { TradingChart } from "@/components/trading-chart";

const Page = () => {
  return (
    <main className="h-full w-full overflow-hidden">
      <TradingChart />
    </main>
  );
};

export default Page;
