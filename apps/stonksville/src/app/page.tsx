import { TradingChart } from "@/components/trading-chart";

const Page = () => {
  return (
    <main className="h-dvh w-full overflow-hidden">
      <TradingChart />
    </main>
  );
};

export default Page;
